<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/frameLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/layout_backgroung_alice_blue"
    android:orientation="vertical"
    android:padding="@dimen/mid_padding">

    <TextView
        android:id="@+id/textViewArea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/layout_background_light_sky_blue"
        android:gravity="center"
        android:paddingStart="@dimen/min_padding"
        android:paddingEnd="@dimen/min_padding"
        android:singleLine="true"
        android:text="@string/shyamoli"
        android:textSize="@dimen/sub_title_text" />

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/mid_margin"
        android:background="@drawable/layout_backgroung_alice_blue"
        app:cardCornerRadius="@dimen/mid_radius"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/imageViewArea"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:background="@drawable/layout_backgroung_alice_blue"
            android:scaleType="centerCrop" />
    </androidx.cardview.widget.CardView>

</LinearLayout>
